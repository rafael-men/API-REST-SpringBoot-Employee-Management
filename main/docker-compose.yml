version: '3.8'

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8000:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://yamanote.proxy.rlwy.net:24827/railway
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: tNWnwDFxZHNXylqTAkfSkxshudgYJmGa
    depends_on:
      - postgres

  postgres:
    image: postgres:15.4
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: tNWnwDFxZHNXylqTAkfSkxshudgYJmGa
      POSTGRES_DB: railway
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
